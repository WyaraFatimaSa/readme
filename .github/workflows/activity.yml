name: Atualizar README com Atividades

on:
  schedule:
    - cron: '0 * * * *' 
  workflow_dispatch:   
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: JamesIves/github-activity-readme@v0.2.2
        with:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
